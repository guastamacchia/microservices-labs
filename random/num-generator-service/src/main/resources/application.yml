spring:
  application:
    name: num-generator-service

server:
  port: 0

maxint: 100

eureka:
  client:
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/

ribbon:
  eager-load:
    enabled: true
  eureka:
    enabled: true
